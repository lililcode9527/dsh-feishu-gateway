# DSH-Feishu-Gateway 使用文档

> 手机飞书直连电脑上的 **DeepSeek Harness**：对话、下达任务、处理授权审批、查看进度与产物，并支持 Agent 主动推送消息到手机。
> 版本：0.1.0 · 更新：2026-08

---

## 1. 项目简介

```
手机飞书 App ──发消息──▶ 飞书服务器 ──长连接(WS)──▶ 本机网关进程 ──HTTP/WS──▶ DSH (127.0.0.1:3080)
                              ▲                          │
                              └──────────回复/审批/推送───┘
```

**架构特点**
- **独立进程**：网关是跑在你电脑上的独立 Node.js 程序，只调用 DSH 公开本地 API，**零侵入**——网关挂了不影响 DSH，DSH 出问题网关会重连。
- **免公网**：网关主动出站连接飞书（长连接 WebSocket），**不需要公网 IP、不开放任何端口**，手机与电脑不在同一网络也能用。
- **功能闭环**：文本对话、真实任务执行、**授权审批（卡片按钮）**、提问回填、进度推送、图片/文件产物推送、会话池、工作区切换、多机器人、白名单、**feishu_send（Agent 主动推送）**。

---

## 2. 交付包内容

```
DSH-Feishu-Gateway-交付包/
├── README.md                  本文件（快速上手）
├── 使用文档.md                本文档（完整说明）
├── feishu-gateway/            网关主项目
│   ├── bin/                   CLI 入口（dsh-feishu-gateway）
│   ├── src/                   源码（index/relay/feishu/dsh/store/bots/send-server…）
│   ├── scripts/               Windows 运维脚本（自启/守护/配置向导）
│   ├── test/                  单元测试（relay + send-server）
│   ├── dsh-feishu-send/       伴生 DSH 插件（feishu_send 工具）
│   ├── .env.example           配置模板（复制为 .env 后填写）
│   ├── bots.example.json      多机器人配置模板
│   ├── start-gateway.bat      一键启动
│   └── package.json / LICENSE
├── docs/                      设计文档（方案分析/飞书后台清单/验收清单/对比/发布计划）
└── tools/                     调试脚本（API 探测）
```

> ⚠️ 交付包已**剔除敏感文件**：不含 `.env`（真实 App Secret）、`data/`（会话数据/令牌）、`node_modules`。使用前需自行 `npm install` 并创建 `.env`（见第 4 节）。

---

## 3. 快速开始（约 10 分钟）

### 步骤 0：前置条件
- 电脑已运行 **DSH Web**（浏览器能打开 `http://127.0.0.1:3080`）
- 已安装 **Node.js ≥ 20**

### 步骤 1：飞书后台创建应用（一次性）
按 `docs/feishu-setup-checklist.md` 操作，要点：
1. [open.feishu.cn](https://open.feishu.cn) → 开发者后台 → **创建企业自建应用**
2. 添加**机器人**能力
3. 开通权限：`im:message`、`im:message:send_as_bot`
4. 事件订阅选**「使用长连接接收事件」**，订阅 `im.message.receive_v1`
5. **创建版本并发布**
6. 复制 **App ID** 和 **App Secret**

> 也可运行 `npm run setup` 自动打开后台并打印步骤清单。

### 步骤 2：安装依赖并配置
```bash
cd feishu-gateway
npm install
copy .env.example .env     # Windows；Linux/macOS 用 cp
# 编辑 .env，填入 FEISHU_APP_ID / FEISHU_APP_SECRET
```

### 步骤 3：自检与上线验收
```bash
npm run check        # 快速自检：DSH 可达 + 凭据有效
npm run verify       # 上线验收：真正建立飞书长连接（看到 ✓ 即通过）
npm start            # 启动网关（或双击 start-gateway.bat）
```
看到 `[feishu] long-connection started (WS)` 即成功。

### 步骤 4：手机实测
飞书 App → 搜索你的机器人 → 发 `你好` → 预期收到 `🤖 已收到，开始处理…` → 介绍回复。
逐项验收见 `docs/mobile-acceptance-checklist.md`。

---

## 4. 详细配置

### 4.1 `.env`（单机器人模式）

| 变量 | 必填 | 说明 |
|---|---|---|
| `DSH_BASE_URL` | 是 | DSH 本地地址，默认 `http://127.0.0.1:3080` |
| `FEISHU_APP_ID` | 是 | 飞书应用 App ID（`cli_` 开头） |
| `FEISHU_APP_SECRET` | 是 | 飞书应用 App Secret（**勿泄露**） |
| `DSH_BOT_NAME` | 否 | 机器人名（用于工作区命名，默认 `default`） |
| `ALLOWED_OPEN_IDS` | 否 | 白名单 open_id（逗号分隔）；留空=不限制 |
| `DSH_SESSION_CWD` | 否 | 机器人会话工作目录（建议指向 DSH 工作区根目录） |
| `SESSIONS_FILE` | 否 | 会话池持久化文件（默认 `data/sessions.json`） |
| `MAX_REPLY_CHARS` | 否 | 单条回复截断长度（默认 4000） |
| `PROGRESS_THROTTLE_MS` | 否 | 进度推送最小间隔（默认 10000ms） |
| `MAX_FILE_PUSH` / `MAX_FILE_BYTES` | 否 | 每轮推送文件数（默认 3）/ 单文件上限（默认 20MB） |
| `TURN_TIMEOUT_MS` | 否 | 单轮任务超时（默认 30 分钟） |
| `FEISHU_SEND_PORT` | 否 | feishu_send 收件口端口（默认 3180） |
| `DSH_FEISHU_CONFIG_DIR` | 否 | 配置目录（默认当前目录） |

### 4.2 多机器人 / 多工作区（`bots.json`）
复制 `bots.example.json` 为 `bots.json`：
```json
[
  {
    "name": "home-bot",
    "appId": "cli_xxx",
    "appSecret": "xxx",
    "workspace": "C:\\Users\\you\\Desktop\\harness",
    "allowedOpenIds": []
  }
]
```
存在 `bots.json` 时忽略 `.env` 的单机器人配置。每个机器人：独立长连接、独立工作区（会话进入对应 **DSH 工作区**，与 GUI 隔离）、独立会话池与白名单。

### 4.3 feishu_send（Agent 主动推送）
网关内置收件口（`127.0.0.1:3180`，令牌保护）。安装伴生插件到 DSH web profile：
```bash
cd 交付包/feishu-gateway
dsh plugin --profile web add "file:C:/完整/路径/dsh-feishu-send"   # 需 pnpm；或手动复制到 ~/.dsh/profiles/node_modules/ 并在 cordis.patch.yml 注册
# 重启 dsh web 生效
```
插件自动读取 `~/.dsh-feishu-gateway.json`（网关启动时写入）或环境变量 `DSH_FEISHU_SEND_URL` / `DSH_FEISHU_SEND_TOKEN`。装好后 Agent 可调用工具 `feishu_send(text, appName?, openId?)`。

---

## 5. 手机端使用说明

| 操作 | 说明 |
|---|---|
| 直接发消息 | 交给电脑上的 DSH 执行，回复为 **Markdown 卡片**（渲染后显示） |
| 授权/审批 | 收到授权卡片，点「✅ 允许 / 🚫 拒绝」按钮（或直接回复 允许/拒绝） |
| 提问 | 收到「❓」，回复编号（如 `1`）或直接输入答案 |
| 进度 | 任务进行中收到「⏳ 进行中：已执行 N 步，正在调用：工具…」 |
| 图片/文件产物 | 自动推送到飞书（图片 ≤10MB；文件默认每轮 ≤3 个、单个 ≤20MB） |
| `/new` | 开启新会话（当前工作区） |
| `/switch <序号>` | 切换会话（`/list` 查看编号） |
| `/workspace` | 查看工作区；`/workspace <序号\|名称>` 切换；`/workspace <目录>` 收养新工作区 |
| `/list` | 列出**未归档**会话（含标题、工作区、编号） |
| `/stop` | 停止当前任务 |
| `/help` | 帮助 |

**要点**
- 每个飞书聊天拥有独立**会话池**，新会话进入当前工作区
- 归档的会话在电脑侧边栏隐藏，手机 `/list` 也不会显示（仍可通过 `/switch` 使用已归档的当前会话）
- 电脑端 GUI 与手机共享同一批 DSH 会话/工作区，可接力同一任务

---

## 6. 运维

| 命令 | 作用 |
|---|---|
| `npm run setup` | 配置向导（打印清单 + 打开飞书后台） |
| `npm run check` | 快速自检（DSH + 凭据） |
| `npm run verify` | 上线验收（真实建立飞书长连接） |
| `npm start` | 启动网关 |
| `npm test` | 运行单元测试 |
| `start-gateway.bat` | 双击启动（先自检再启动） |
| `scripts/install-autostart.ps1` | 注册登录自启（启动文件夹，免管理员） |
| `scripts/uninstall-autostart.ps1` | 移除自启 |
| `scripts/run-gateway.ps1` | 守护进程（崩溃自动重启，日志 `data/gateway.log`） |

**注意**
- 自启与手动启动**二选一**，避免两个网关实例同时连飞书
- 网关需电脑开机运行；重启后 `start-gateway.bat` 一键恢复

---

## 7. 安全与隐私（必读）

1. **App Secret 是唯一钥匙**：泄露 = 别人可完全控制你电脑上的 DSH。不要提交到仓库/聊天/网盘。
2. `.env`、`data/`（会话映射、feishu_send 令牌）已在 `.gitignore` / `.npmignore`，**不会进入 git 或 npm 包**；交付包也**不含**这些文件。
3. **重置 App Secret**：如担心泄露，飞书后台「凭证与基础信息」→ 重置 → 更新 `.env` → 重启网关（白名单 open_id 不变）。
4. **不要在该项目目录里运行 `dsh` 命令**：`.env` 的 `DSH_BASE_URL` 与 dsh 保留变量冲突，dsh 会拒绝启动。请从其他目录运行 `npx @deepseek-ai/dsh ...`。
5. feishu_send 收件口只监听 `127.0.0.1` 且需令牌，勿改成公网监听。

---

## 8. 故障排查

| 现象 | 大概率原因 | 解决 |
|---|---|---|
| `verify` 超时/失败 | App Secret 错误 / 应用未发布 / 后台未选长连接 | 对照 `docs/feishu-setup-checklist.md` 重查并重新发布 |
| 网关启动但收不到消息 | 事件未订阅 `im.message.receive_v1`，或订阅方式仍是 Webhook | 后台事件与回调改为长连接并重新发布 |
| 能收不能回 | `im:message` / `im:message:send_as_bot` 权限未生效 | 开通权限并发布版本 |
| 机器人搜不到 | 可用范围不含你 / 未发布 | 版本管理与发布 → 可用范围选全员并发布 |
| 回复是 Markdown 源码 | 网关为旧版本 | 重启网关（新版本回复为卡片） |
| `feishu_send` 报 gateway not reachable | 网关未运行 / 插件找不到配置 | 启动网关；检查 `~/.dsh-feishu-gateway.json` 或设环境变量 |
| 某旧会话无 `feishu_send` 工具 | 会话早于插件安装 | 开新会话 |
| 文件没推送 | 文件超限 / 路径在沙箱外 / 仅工作区配置的旧网关 | 看网关日志 `[relay] file push`；重启网关 |
| 审批不弹卡片 | 该操作未触发审批策略（策略相关） | 属正常；审批逻辑已单测覆盖 |

---

## 9. 测试与发布

```bash
cd feishu-gateway
npm test                 # 单元测试（relay 35 + send-server 7）
node dsh-feishu-send/test/plugin.test.mjs   # 插件测试
npm run smoke            # 真实 DSH 冒烟（dry-run 对话/提问/工具/进度/产物）
```

**发布到 npm**（可选，需 npm 账号）：
```bash
npm pack --dry-run       # 预览发布内容（确认无 .env / data）
npm login --registry https://registry.npmjs.org
npm publish --registry https://registry.npmjs.org
```
详见 `docs/publish-plan.md`。

---

## 10. FAQ / 路线图

**已实现**：对话、任务、审批卡片、提问、进度、图片/文件推送、会话池、工作区切换与隔离、多机器人、白名单、Markdown 卡片回复、feishu_send、归档过滤、消息去重、断线重连、自启守护、CLI、单测 50+。

**规划中**：扫码创建机器人（免手动建应用）、OnIt 表情（需 `im:message.reaction` 权限）、配置热加载。

**常见疑问**
- 会话会出现在电脑侧边栏吗？→ 取决于工作区：共享工作区会混排（可接力），独立工作区则分开展示。
- 手机和电脑必须同一 Wi-Fi 吗？→ 不需要（长连接走公网出站）。
- 需要开放端口吗？→ 不需要。
