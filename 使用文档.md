# DSH-Feishu-Gateway 使用文档（插件版）

> 手机飞书直连电脑上 **DeepSeek Harness** 的**桌面当前会话**：对话、任务、审批（卡片）、提问、进度、图片/文件产物、Agent 主动推送（feishu_send）。
> 形态：**DSH 深度集成插件**（`dsh-feishu-gateway-plugin`），随 dsh web 启动，无需独立进程。
> 版本：0.3.0 · 更新：2026-08

---

## 1. 架构与原理

```
手机飞书 App ──发消息──▶ 飞书服务器 ──长连接(WS)──▶ dsh-feishu-gateway-plugin（DSH 进程内）
                                                      │
                                                      ├─▶ 桌面当前 Agent 会话（ctx.agents 解析/恢复）
                                                      │     回复经 agent.session.events 提取 → 飞书卡片
                                                      ├─▶ 提问/审批：api-proxy mux → 飞书卡片/编号 → /api/respond
                                                      ├─▶ feishu_send 工具（原生注册）
                                                      └─▶ 管理路由 /feishu/admin/* + 设置面板
```

- **会话模型**：手机消息**直接进入电脑端当前打开的会话**（bot 工作区内最近活动），手机与桌面同一对话；不建专用会话。
- **免公网**：长连接出站，不开放任何端口。
- **多机器人**：`~/.dsh-feishu/config.json` 的 `bots` 数组。

## 2. 安装（一次性）

前置：DSH Web 运行中（127.0.0.1:3080）、Node.js ≥ 20、飞书后台已建应用（`docs/feishu-setup-checklist.md`）。

```bash
# 1) 装 pnpm（首次）
npm install -g pnpm

# 2) 安装插件到 web profile（注意：不要在 feishu-gateway 相关目录跑 dsh 命令；从主目录执行）
cd C:\Users\你的用户名
dsh plugin --profile web add "file:<本仓库绝对路径>/dsh-feishu-gateway-plugin"

# 3) 若提示 protobufjs 构建被忽略（ERR_PNPM_IGNORED_BUILDS）：
#    编辑 ~/.dsh/profiles/web/pnpm-workspace.yaml，把 allowBuilds.protobufjs 设为 true，重跑第 2 步

# 4) 配置 ~/.dsh-feishu/config.json：
{ "bots": [{ "name": "ds-hs", "appId": "cli_xxx", "appSecret": "xxx", "workspace": "C:\\Users\\you\\Desktop\\harness", "allowedOpenIds": [] }] }

# 5) 重启 dsh web → 日志出现 [feishu-gw] plugin active 与 long connection ready
```

> 手动安装（无 pnpm）备选：把 `dsh-feishu-gateway-plugin` 复制到 `~/.dsh/profiles/node_modules/`，并在 `~/.dsh/profiles/web/cordis.patch.yml` 加：
> ```yaml
> - insert:
>     - id: feishu-gateway-plugin
>       name: dsh-feishu-gateway-plugin
> ```

## 3. 配置（~/.dsh-feishu/config.json）

| 字段 | 必填 | 说明 |
|---|---|---|
| `bots[].name` | 否 | 机器人显示名（默认 appId） |
| `bots[].appId` / `appSecret` | 是 | 飞书应用凭据（**勿泄露**） |
| `bots[].workspace` | 否 | 该机器人使用的工作区目录（手机消息进入此工作区内电脑端当前会话）；留空=沙箱根目录 |
| `bots[].allowedOpenIds` | 否 | 白名单 open_id 数组；留空=不限制 |
| `bots[].ownerOpenId` | 否 | 归属人 open_id（扫码创建自动回填；掉线告警发往此用户） |
| `bots[].targetSessionId` | 否 | 固定消息目标会话（一般由 `/switch` 写入；留空=自动跟随电脑端当前会话） |

配置**热加载**（每 5 秒检查，改动无需重启）；也可用设置面板（见第 6 节）或 `/feishu/admin/config`。

## 4. 使用（手机端）

| 操作 | 说明 |
|---|---|
| 直接发消息 | 进入**电脑端当前打开的会话**（或 `/switch` 固定的会话），桌面可见（带 `[飞书 ou_...]` 前缀）；回复以 Markdown 卡片推回手机 |
| 发图片/文件 | 图片交给 Agent 看图；文件存入工作区 `.feishu-uploads/<日期>/` 并交给 Agent 读取处理 |
| 授权/审批 | 收到授权卡片，点「✅ 允许 / 🚫 拒绝」（或回复 允许/拒绝） |
| 提问 | 收到「❓」，回复编号（如 `1`）或直接输入答案 |
| 进度 | 任务进行中收到「⏳ 进行中：已执行 N 步，正在调用：工具…」（同一张卡片原地更新） |
| 图片/文件产物 | 自动推送到飞书（图片 ≤10MB；文件默认每轮 ≤3 个、单个 ≤20MB） |
| 掉线告警 | 飞书连接断开/恢复/异常时，机器人主动私聊归属人（ownerOpenId） |
| `/list` | 查看所有工作区及会话（同电脑端侧边栏，▶️ 标当前消息目标，带编号） |
| `/switch` | 切换消息目标会话：`/switch <编号>` 固定，`/switch`（无编号）恢复自动跟随电脑端当前会话 |
| `/model` | 查看当前模型与可用列表；`/model <编号>` 切换模型（下一条消息生效） |
| `/timer <分钟> <内容>` | 定时提醒（如 `/timer 10 提醒我喝水`） |
| `/cancel` | 取消本聊天的定时提醒并中断当前回合 |
| `/help` | 帮助 |

## 5. feishu_send（Agent 主动推送）

插件**原生注册** `feishu_send` 工具。任意 Agent 会话里：
- `feishu_send(text)` — 发给最近与该 bot 聊天的会话
- `feishu_send(text, chatId)` / `feishu_send(text, appId)` — 指定目标/机器人

消息以 Markdown 卡片推送。无需独立进程、无需收件口。

## 6. 设置面板与扫码建机器人

浏览器打开 `http://127.0.0.1:3080/feishu/admin/panel`：
- 机器人列表（名称/工作区/AppID/密钥/连接状态/最近会话）
- 添加/编辑/删除机器人、测试发送
- **扫码创建机器人**：点「生成二维码/验证链接」→ 飞书 App 扫码确认 → App ID/Secret 自动回填 → 保存

管理 API：`/feishu/admin/status`、`/config`、`/delete-bot`、`/send-test`、`/onboard`、`/onboard/poll`（仅 127.0.0.1）。

## 7. 故障排查

| 现象 | 原因/解决 |
|---|---|
| 启动无 `[feishu-gw] plugin active` | 插件未注册/未重启；检查 bundles 与 cordis.patch.yml |
| `long connection` 未 ready | 凭据错误/应用未发布/后台未选长连接；对照 `docs/feishu-setup-checklist.md` |
| 手机消息没进当前会话 | 确认 config 的 workspace 与桌面所在工作区一致；或 `/switch` 已固定了别的会话（发 `/switch` 恢复自动跟随） |
| 提问/审批没到手机 | 该会话须先有飞书消息（sessionOwner 映射）；否则显示在 GUI |
| feishu_send 报"没有可用 chat_id" | 先给机器人发条消息，或显式传 chatId |
| 旧插件冲突（feishu_send 注册两次） | 删除旧 dsh-feishu-send，只保留本插件 |

## 8. 开发与测试

```bash
cd dsh-feishu-gateway-plugin
npm test     # 单测（需测试桩 node_modules/@deepseek-ai/dsh-tools）
```
结构：`index.js`（宿主插件）、`lib/feishu.js`（长连接/发送）、`lib/dsh.js`（/api 客户端）、`lib/admin-panel.js`（设置面板）、`cordis.patch.yml`（自注册补丁）。

## 9. 安全

- App Secret 只存于 `~/.dsh-feishu/config.json`（本机），勿提交仓库
- 管理路由仅监听 127.0.0.1
- 白名单：`allowedOpenIds` 限定可用用户
